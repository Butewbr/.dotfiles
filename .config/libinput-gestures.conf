# 4 fingers vertical - volume control
gesture swipe up 4 xdotool key XF86AudioRaiseVolume
gesture swipe down 4 xdotool key XF86AudioLowerVolume

# 4 finger tap - mute/unmute (you'll need to map this)
# Note: libinput-gestures doesn't natively support taps, see below

# 3 fingers pinch - floating windows
# Note: Pinch gestures and 2-finger resize are tricky, see below